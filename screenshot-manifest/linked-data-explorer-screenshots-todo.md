# Linked Data Explorer — screenshots to capture (v1.9.7 → v1.9.11 sync)

Generated by `/iou-document-patch` on 2026-07-08 for the documentation sync from
Linked Data Explorer **v1.9.7** to **v1.9.11**.

Real screenshot files live in **`docs/assets/screenshots/`** (language-neutral,
served at the site root). Docs reference them as
`../../assets/screenshots/<file>` inside a `<figure markdown>` block. Capture
each image below at the same framing/width as the existing set and drop it in
that folder — the doc references are already in place.

Legend: **NEW** = no file exists yet · **REPLACE** = file exists but now shows
stale UI.

| # | Status | File | Embedding page(s) | What it must show | Trigger |
|---|---|---|---|---|---|
| 1 | **REPLACE** | `linked-data-explorer-bpmn-deploy-modal.png` | `user-guide/bpmn-modeler.md`, `features/bpmn-modeler.md` | The deploy modal now including the required **Board ownership** section — the auto-detected board with an override control — above the Operaton endpoint field | v1.9.9 |

## Notes

- The gap's other changes are non-visual: v1.9.8 is CPRMV SHACL shapes, v1.9.10
  is the `/v1/norms ?cprmv_version=` API selector, and v1.9.11 is a deploy
  bug-fix — none change a documented screen, so only the deploy modal needs a
  re-shot.
- v1.9.10 was already documented before this sync (`developer/backend.md`,
  `reference/api-stability.md`); this run only added its changelog entry and
  cross-links, so it introduces no new figures.
- No NL screenshots are needed — the NL LDE pages are placeholders and do not
  embed screenshots.
