# CPSV Editor — screenshots to capture

*A running record across syncs, newest first. Last reviewed for v2026.09.6 on
19 September 2026 — **nothing outstanding**; that review asked for no captures.*

Generated by `/iou-document-patch` on 2026-08-18 for the documentation sync from
CPSV Editor **v1.10.6** to **v2026.08.0** (covering v1.10.7, v2026.07.0 and
v2026.08.0).

Real screenshot files live in **`docs/assets/screenshots/`** (language-neutral,
served at the site root). Docs reference them as
`../../assets/screenshots/<file>` inside a `<figure markdown>` block. Capture each
image below at the same framing/width as the existing set and drop it in that
folder — the doc references are already in place.

Legend: **NEW** = no file exists yet · **REPLACE** = file exists but now shows
stale UI.

| # | Status | File | Embedding page(s) | What it must show | Trigger |
|---|---|---|---|---|---|
| 1 | ✅ **DONE** (was NEW) | `cpsv-editor-dmn-api-configuration.png` | `user-guide/dmn-workflow.md` (Step 3) | The **API Configuration** panel: the Base URL field, and the **Evaluation URL preview resolving to a Linked Data Explorer backend endpoint** (`…/v1/dmns/evaluate/{decisionKey}`) rather than an Operaton `/engine-rest/…` URL | v2026.08.0 |
| 2 | ✅ **DONE** (was REPLACE) | `cpsv-editor-dmn-tab.png` | `features/dmn-orchestration.md` | DMN File card whose deploy/evaluate controls and URL preview reflect the backend route | v2026.08.0 |
| 3 | **REPLACE** (optional) | `cpsv-editor-concepts-tab.png` | `user-guide/filling-in-the-tabs.md` | Concepts tab listing an **output concept that survived an empty evaluate result** — i.e. outputs present alongside the inputs even though Evaluate Decision returned no match | v2026.08.0 |

**Outstanding: item 3 only**, and it is optional — the current image is not
wrong, it simply does not demonstrate the empty-result fallback the page now
describes.

## Notes

- Items 1 and 2 were captured on 19 August 2026 and are already in
  `docs/assets/screenshots/`. A non-strict `mkdocs build` now reports **zero**
  missing-image warnings — any image warning that appears is a real broken
  reference, not an expected gap.
- **Cell-level legislative grounding has deliberately no screenshot.** It is a
  change to the generated Turtle rather than to the interface, so
  `developer/cell-level-grounding.md` documents it with worked TTL examples
  instead of a figure. Do not add a manifest row for it.
- The **Testing** page (`developer/testing.md`) is likewise prose, tables and
  commands only — nothing to capture.
- No NL screenshots are needed — the NL CPSV Editor pages are placeholders and
  embed no images. Only `developer/due-diligence.md` is a full translation, and
  it carries no figures.

## Sync v2026.09.4 → v2026.09.6 — no screenshots needed

Reviewed on 19 September 2026 for the v2026.09.5 and v2026.09.6 gap. **Nothing to
capture**, by design rather than oversight:

- **Both user-visible changes restore intended behaviour without changing the UI.** A
  DSO import now opens the DMN tab with the model in it, which is what the existing
  prose and figures already showed; before the fix the panel was empty, which no
  screenshot ever documented. Backend errors now show the server's reason in the same
  banners and panels that carried the generic text.
- **Everything else is tooling.** The dependency check before `npm start` and before a
  push, the release-time mirror and preview checks, the preview-closing workflow, the
  Renovate rule and the Semgrep suppressions have no visible surface in the editor.
- **The DMN tab's Base URL field is unchanged.** This review found its documented
  meaning was wrong — it decides the published `cprmv:implementedBy`, not the deploy
  target — and corrected that in prose; the field itself looks as it did.

## Sync v2026.09.2 → v2026.09.4 — nothing to capture

Reviewed on 11 September 2026 for v2026.09.3 and v2026.09.4. No view changed:

- **Semgrep, lock-file maintenance and the Renovate rules** are CI and repository
  configuration — workflow YAML, `renovate.json`, a ruleset.
- **The two iKnow guards** reject configurations the bundled mappings never contain;
  the Vendor tab renders exactly as before. A rejected transform surfaces as an
  error, and there is no bundled configuration that produces one to photograph.
- **The TTL export fix** changes what is written for twelve of 216 rules in one
  export, not anything on screen.
- **The two example models** are repository content, loaded through the existing
  import and DMN tab.
- **`E2E_BASE_URL`** is a test-runner variable.

`cpsv-editor-vendor-tab.png` still shows the tab accurately — the vendor form and
certification section it depicts did not change.

## Sync v2026.08.3 → v2026.09.0 — nothing to capture

Checked on 4 September 2026 for the sync covering **v2026.09.0**, and **no rows
were added**.

The release is 26 commits typed `chore` (14), `docs` (5), `ci` (5) and `fix` (2)
— **not one `feat`**. It is entirely pipeline, supply-chain, dependency and
release-process work: the audit gate widened to every pull request and taught to
validate `renovate.json`, deploy workflows renamed and path-filtered, Renovate
capped below the staging ceiling, merge method moved into repository settings,
line endings normalised, and the Create React App to Vite migration planned.

None of that alters a view the documentation depicts. The application's
interface is byte-identical to v2026.08.3 — the dependency majors it took
(React 19.2.8, `lucide-react` 1.33.0, two testing-library majors) moved no test
and no coverage figure, which was the point of taking them before the migration
rather than during it.

**Item 3 below remains the only outstanding entry**, and it is still optional.

---

## Sync v2026.08.1 → v2026.08.3 — nothing to capture

Checked on 28 August 2026 for the sync covering **v2026.08.2** and
**v2026.08.3**, and **no rows were added**.

Both releases are entirely CI, supply-chain and release-process work: digest
pinning, least-privilege `permissions:`, a blocking zizmor `audit` job, Renovate
under a cooldown, the `acc` branch ruleset, and the change that makes releases
land through a pull request. None of it alters a view the documentation depicts.

The one user-visible change is a `ci` icon in the in-app Changelog tab. No page
documents that tab, so under the manifest's own rule — a **NEW** row obliges a
`<figure>` on a page, and a row with no embedding page is a defect — there is
nothing to add. The new [Supply-Chain Pinning](../docs/en/contributing/supply-chain.md)
page is prose, tables and a policy snippet only.

**Item 3 below remains the only outstanding entry**, and it is still optional.

---

## Previous sync (v1.10.2 → v1.10.6) — all captured

Retained for reference; nothing outstanding. The two NEW items from that round,
`cpsv-editor-cprmv-version-selector.png` and `cpsv-editor-parameters-tab.png`,
are both present in `docs/assets/screenshots/`, as are the REPLACE targets
`cpsv-editor-test-cases.png`, `cpsv-editor-concepts-tab.png` and
`cpsv-editor-main-ui.png`.
